import { api } from "@/lib/api-client";
import { Book } from "@/types/book";

class BookService {
    // async createBook(data: Book): Promise<>{
    //     return api.post('/books/create', data)
    // }
}
