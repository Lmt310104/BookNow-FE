import { Button } from "@/components/ui/button";
function HomePage() {
    return (
        <div>
            <h1>Welcome to the Home Page</h1>
            <Button variant="default">Navigate to login</Button>
        </div>
    );
}

export default HomePage;